{
  "$schema": "https://code.claude.com/schemas/marketplace.json",
  "name": "agentx402",
  "owner": {
    "name": "agentx402",
    "url": "https://agentx402.ai"
  },
  "plugins": [
    {
      "name": "agentkv",
      "source": {
        "source": "git-subdir",
        "url": "agentx402-ai/agentkv",
        "path": "plugin/agentkv",
        "ref": "v0.4.3"
      },
      "description": "Agent-native encrypted KV store paid via x402. Persistent cross-session memory, encrypted secret storage, and shared fleet state — keyed by wallet address, zero-knowledge to the server."
    },
    {
      "name": "agentscout",
      "source": {
        "source": "git-subdir",
        "url": "agentx402-ai/agentscout",
        "path": "plugin/agentscout",
        "ref": "v0.5.3"
      },
      "description": "Agent-native web read/extract/crawl paid via x402. Fetch any URL to clean markdown, extract structured JSON against a caller schema, or crawl a site — pay per fetch in USDC, no accounts or API keys."
    },
    {
      "name": "agentrag",
      "source": {
        "source": "git-subdir",
        "url": "agentx402-ai/agentrag",
        "path": "plugin/agentrag",
        "ref": "v0.1.8"
      },
      "description": "AgentRAG: x402-paid retrieval-augmented generation for agents. Ingest your own documents or URLs into a collection (hybrid BM25 + vector search), then ask grounded questions against them — paid per call in USDC, no accounts or API keys."
    }
  ]
}
